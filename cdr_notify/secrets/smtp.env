# SMTP configuration for cdr_notify

# SMTP server host
SMTP_SERVER=smtp.example.com

# SMTP port (587 for STARTTLS, 465 for SSL, 25 for plain)
SMTP_PORT=587

# SMTP authentication credentials
SMTP_USERNAME=your_smtp_username
SMTP_PASSWORD=your_smtp_password

# Sender email address (optional; defaults to SMTP_USERNAME or EMAIL_TO_SEND)
SMTP_FROM=alerts@example.com

# TLS/SSL flags
SMTP_USE_TLS=true
SMTP_USE_SSL=false

# CDR folder to monitor
CDR_FOLDER=/path/to/cdr/folder

# Recipient email address for notifications
EMAIL_TO_SEND=recipient@example.com

# Optional: log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
